[package]
name = "codequery-lsp"
version = "0.1.0"
edition = "2021"

[dependencies]
codequery-core = { path = "../codequery-core" }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = { workspace = true }
