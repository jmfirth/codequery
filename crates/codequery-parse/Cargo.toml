[package]
name = "codequery-parse"
version = "0.1.0"
edition = "2021"

[dependencies]
tree-sitter = "0.24"
tree-sitter-rust = "0.23"
codequery-core = { path = "../codequery-core" }

[dev-dependencies]
tempfile = { workspace = true }
