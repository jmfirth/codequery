# CSV symbol extraction rules for cq.
#
# CSV has minimal structure; extract column headers from the first row.

[language]
name = "csv"
extensions = [".csv", ".tsv"]

# Column headers from the header row
[[symbols]]
kind = "field"
query = '(header (field) @name) @def'
name = "@name"
body = "@def"
