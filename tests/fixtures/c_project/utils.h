#ifndef UTILS_H
#define UTILS_H

/* Add two integers. */
int add(int a, int b);

/* Multiply two integers. */
int multiply(int a, int b);

typedef int (*operation_fn)(int, int);

#endif /* UTILS_H */
